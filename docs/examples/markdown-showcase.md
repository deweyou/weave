# Markdown 全集 · Weave 效果手册

这是一份可以直接导入 Weave 的长期展示样本。你可以在正文中修改表格、勾选任务、编辑代码，再导出 Markdown 对照原文。

这里的“全集”覆盖常用 Markdown、GitHub 风格写法和常见扩展，不代表所有方言都已实现。前半部分展示当前能力，后半部分保留兼容性对照；功能状态以当前源码和实际操作为准。

## 先看效果

把想法写成 **清晰的文字**，用 *一点强调* 标出重点，让 ~~已经过时的内容~~ 留下修改痕迹。

| 内容 | 状态 | 数量 |
| :--- | :---: | ---: |
| 表格原位编辑 | 可以体验 | 3 |
| 代码语言与高亮 | 可以体验 | 6 |

```swift
struct Note {
    let title = "你好，Weave 👋"
    var isPinned = false
}
```

> 写作先保持流畅，格式随后跟上。

- [x] 导入这份文档
- [ ] 修改上方表格的状态
- [ ] 点击代码块，切换语言或复制代码

## 01 · 六级标题

以下是真实标题，不是代码示意；观察字号、字重和上下留白。

# 一级标题 · 一次新的出发

一级标题适合文档主标题。

## 二级标题 · 把事情讲清楚

二级标题组织主要章节。

### 三级标题 · 一个可执行的步骤

三级标题适合章节中的小节。

#### 四级标题 · 补充说明

四级标题强调局部主题。

##### 五级标题 · 细节提示

五级标题呈现更轻的层级。

###### 六级标题 · 旁注与说明

六级标题仍然是标题，不应被误识别成粗体正文。

## 02 · 段落、中英文与 Unicode

这是普通中文段落。Weave 使用系统字体，正文保持稳定的阅读宽度。中文标点应自然呈现：逗号、顿号，句号。括号（补充说明）、引号“这是一段引用”和破折号——都不需要额外的 Markdown 标记。

This is an English paragraph. Mix 中文、English、1234567890 and punctuation in the same sentence. Observe the baseline, wrapping, selection, and caret position.

这段较长的文字用于观察自然换行。在较窄的窗口里，文字应沿着可用宽度连续排布，不产生横向溢出；放大窗口后，正文仍保持适合阅读的宽度，而不是把一整句话拉到屏幕两端。你可以在句子中间插入文字，选择一段内容，再尝试撤销和重做。

Emoji：🙂 🚀 ✅ 🌏 👩🏽‍💻 👨‍👩‍👧‍👦 🇨🇳 🇺🇸。

组合字符：café / café。选中后删除、撤销，观察字形和选区是否完整。

这是相邻的第一行。
这是相邻的第二行。

这是空一行后的新段落。

在编辑器中，上一组相邻源码行属于同一段；这类段内换行不增加段距。新段落额外增加 6 pt 段后距。Mac 按 Enter 换段，Shift+Enter 段内换行。


这段之前有两个源码空行：一个用于分段，另一个保留为可编辑空段。可以在空段中输入、删除，观察下方文字的位置是否稳定。

## 03 · 行内格式

**这是加粗文字**，前后仍然是普通正文。

*这是斜体文字*，适合补充语气。

***这是粗斜体文字***，同时观察两种字形特征。

~~这项计划已经取消~~，后面的文字不应继续带删除线。

**中文 English 123** 与 *中英文混排*，以及 ~~旧版本 v0.1~~。

行内代码 `let count = 42` 应只给文字本身加底色，不铺满整行。

字面反引号可以放进更长的分隔符：`` `value` ``。

代码中的标记保持字面含义：`**不是加粗**`、`a | b`、`![不是图片](image.svg)`。

这段 **加粗**、*斜体*、~~删除线~~ 与 `代码` 之间都有普通文字。

## 04 · 链接

[普通链接](https://example.com)

[**加粗的链接标签**](https://example.com/guide)

[带空格编码的路径](https://example.com/a%20b)

[邮件地址](mailto:hello@example.com)

链接样例只用于观察标签、编辑和导出，不需要访问对应网站。选中文字后可使用链接菜单编辑或移除链接。

## 05 · 无序列表

- 收集一个想法
- 把想法整理成段落
- 给段落补充 **重点** 和 `关键词`

下面展示三种常见无序列表标记。导入后可能统一成相同的圆点，这是语义转换，不保留源文的标记偏好。

* 星号列表第一项
* 星号列表第二项

+ 加号列表第一项
+ 加号列表第二项

基本缩进示例：

- 阅读计划
    - 原生编辑体验
        - 光标与选区
        - 撤销与重做
    - Markdown 交换
- 发布前检查

当前列表使用文本前缀和基本缩进，不提供完整列表树、跨项拖动或自动重编号。

## 06 · 有序列表与任务列表

1. 创建记录
2. 写下正文
3. 添加表格
4. 导出 Markdown

12. 从指定编号开始
13. 下一项继续递增

- [x] 已完成的任务
- [ ] 尚未开始的任务
- [ ] 包含 **重点** 的任务
    - [x] 一项已完成的子任务
    - [ ] 一项待处理的子任务

勾选框可以点击；它们是文档里的文本标记，不关联独立待办系统。

## 07 · 引用

> 简单的引用，用来容纳一句值得记住的话。
> 第二行延续同一段表达，观察竖线和正文缩进。

> 引用里也可以包含 **加粗**、*斜体* 和 `行内代码`。

引用与后续正文之间应保持清楚的段落边界。

## 08 · 表格

第一行是表头。点击单元格直接输入；表格上方菜单提供增删行列和当前列对齐。Tab 切换单元格，最后一格按 Tab 会增加一行。

### 左、中、右对齐

| 项目 | 优先级 | 进度 |
| :--- | :---: | ---: |
| 编辑体验 | 高 | 80% |
| 本地保存 | 高 | 100% |
| 视觉检查 | 中 | 60% |

### 空单元格与转义字符

| 场景 | 内容 | 备注 |
| --- | --- | --- |
| 空值 |  | 仍可点击编辑 |
| 管道符 | a \| b | 导出后不应多出一列 |
| 反斜线 | C:\\Notes\\draft | 路径只是示例文字 |
| Emoji | 👩🏽‍💻 🚀 | 观察选择和删除 |
| 中文标点 | “你好”，世界。 | 保留原文字面量 |

### 宽表格

窄窗口或手机上横向滚动，观察最右侧单元格是否仍可编辑。

| 任务 | 负责人 | 周一 | 周二 | 周三 | 周四 | 周五 |
| --- | --- | :---: | :---: | :---: | :---: | :---: |
| 文档整理 | 林 | 写作 | 复核 |  |  | 完成 |
| 编辑器验证 | 周 | 输入 | 表格 | 代码 | 导出 | 复查 |

### 单元格内容边界

当前单元格是单行纯文本。下面的粗体、代码、链接标记应作为单元格文字出现，而不是单元格内的富文本。

| 类型 | 字面内容 |
| --- | --- |
| 粗体标记 | **重点** |
| 代码标记 | `value` |
| 链接标记 | [网站](https://example.com) |

当前不支持合并单元格、多行单元格和单元格富文本。

## 09 · 代码块与语言

下面六种语言有基础词法高亮。在代码块顶部选择语言、复制代码；Tab / Shift-Tab 调整缩进，回车延续缩进，空行回车退出代码块。代码只作为文字编辑，不会执行。

### Swift

```swift
// 字符串和注释里的 let 不应变成关键字
struct Draft {
    let title: String
    var wordCount = 128

    func summary() -> String {
        return "Hello, Weave"
    }
}
```

### JavaScript

```javascript
const note = { title: "Hello", done: false };

function describe(item) {
    // A short comment
    return `${item.title}: ${42}`;
}
```

### TypeScript

```typescript
interface Note {
    title: string;
    archived: boolean;
}

const note: Note = { title: "Draft", archived: false };
```

### Python

```python
# Keep indentation when pressing Return
class Notebook:
    def __init__(self):
        self.notes = ["First", "Second"]

    def count(self):
        return len(self.notes)
```

### JSON

```json
{
    "title": "Markdown 示例",
    "count": 42,
    "archived": false,
    "tags": ["writing", "native"],
    "reminder": null
}
```

### Shell

```shell
# 示例代码，不会在编辑器中执行
for name in alpha beta; do
    echo "Hello, $name"
done
```

### 无语言标签

```
普通代码块，不做语法高亮。
**这里保持字面星号**
[这里不是链接](https://example.com)
```

### 未知语言

语言标签应在导入导出时保留；未提供对应规则时按普通代码显示，不应猜测或改写代码。

```sql
SELECT title, updated_at
FROM notes
WHERE archived = false;
```

### 首行为空与内部空行

```swift

let first = "The first line above is intentionally empty"

let second = 2
```

### 代码内容含三反引号

外层使用四个反引号，内层三个反引号应成为代码文字。

````markdown
# 一段 Markdown 源码

```swift
let nested = true
```
````

### 波浪号围栏

~~~python
value = 42
print(value)
~~~

## 10 · 转义与字面标点

这些星号不应变成强调：\*普通文字\*。

这些标记也是正文：\# 不是标题、\- 不是列表、\[不是链接\]、\`不是代码\`。

反斜线本身：\\。

普通路径、括号与标点：notes/draft.md、(draft)、[draft]、a+b、x=y、A_B。

## 11 · 兼容性对照：基础写法的其他形式

这一节用于观察当前差异，不作为已支持能力承诺。随着编辑器完善，逐项把已验证的写法移动到前面的对应章节。

### Setext 标题

下面是下划线式标题；目前没有专门的段落转换规则。

Setext 一级标题
==============

Setext 二级标题
--------------

### 分隔线

目前没有专门的分隔线块，下面保留三种常见写法观察实际显示。

---

***

___

### 深层引用与引用中的列表

目前不提供完整的嵌套块解析。

> 第一层引用
>> 第二层引用
>
> - 引用中的第一项
> - 引用中的第二项

### 缩进式代码

目前的代码块以围栏形式为主；以下四空格缩进作为对照。

    let indented = true
    print(indented)

### 硬换行与自动链接

下面第一行末尾有两个空格；源码维护时保留这两个空格。导入后是同一段内的换行，不额外增加段距；导出后仍保留硬换行语义。

第一行末尾两个空格。  
第二行。

裸地址：https://example.com

尖括号地址：<https://example.com>

### 引用式链接

[引用式链接][sample-reference]

[sample-reference]: https://example.com "Example"

## 12 · 兼容性对照：尚未提供专用渲染的扩展

这些样例故意保留在全集中，方便将来补齐能力时复查。在当前 Weave 中，可能显示为普通文字或普通代码块；其他 Markdown 阅读器的显示不代表 Weave 已支持。

### 图片

当前不显示或编辑图片，下面的图片语法用于观察原文是否保留，不需要下载远程资源。

![Weave 文档示意图](markdown-sample.svg)

### HTML

<span style="color: red">内联 HTML 样例</span>

<details>
<summary>折叠内容样例</summary>
这段内容属于 HTML 折叠区。
</details>

### 脚注

这里有一条脚注引用[^sample-footnote]。

[^sample-footnote]: 这是一条脚注定义。

### 数学公式

行内公式：$E = mc^2$。

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

### Mermaid 图表

```mermaid
flowchart LR
    A[写下想法] --> B[整理内容]
    B --> C[导出 Markdown]
```

### 提示块

> [!NOTE]
> 这是 GitHub 风格提示块示例，当前不会转换成专用提示卡片。

### 常见笔记扩展

==高亮==

H~2~O 与 x^2^

[[双向链接]]

:smile: 与 🙂 的对照。

术语
: 定义列表中的解释。

## 13 · 动手检查

- [ ] 修改一个标题，然后撤销
- [ ] 在中文和 emoji 中间移动光标、替换选区
- [ ] 在列表末尾回车续写，空行回车退出
- [ ] 点击任务标记，再撤销
- [ ] 修改表格单元格，用 Tab 和 Shift-Tab 切换
- [ ] 增删一行或一列，修改对齐，然后撤销
- [ ] 横向滚动宽表格，编辑最后一列
- [ ] 在代码块中续行、缩进、切换语言和复制
- [ ] 选中包含表格的正文，剪切、粘贴并撤销
- [ ] 导出 Markdown，再导入成一条新记录
- [ ] 关闭并重新打开 App，核对修改是否保留

## 14 · 维护约定

本文件是展示样本的源文件。Weave 中导入的记录是独立副本；在 App 内修改不会自动写回本文件。需要保留修改时先导出，再对比更新仓库文件。

新增语法时同时补一段正常样例、一段容易出错的边界样例，并核对本期规格中的支持状态。不要为了让展示页看起来完整，把其他 Markdown 阅读器能显示的功能写成 Weave 已实现。

本文件的复选框是手动检查清单，未勾选不表示失败；勾选也不能替代测试和实际操作证据。
