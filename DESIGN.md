# Weave 设计原则

产品目标是安静、直接、可持续写作的个人工作空间。第一期实现本地富文本编辑；后续画板与同步见[产品边界](docs/product.md)。

## 原生优先与审美 skill

用户确认的方向是 Swift 原生、Apple 控件与 Liquid Glass。设计优先级为：内容可读与数据安全 → 稳定输入和无障碍 → 平台惯例 → 视觉精修。

- `apple-design` 用于即时反馈、连续可打断的交互、空间关系、节制的材质与动态效果。其 CSS / Web API 示例需要转译为平台行为，不能成为引入 Web 技术的理由。
- `high-end-visual-design` 用于审核层级、间距、对齐、材质与细节一致性。它面向营销网站的随机布局、字体禁用、双层卡片、超大标题和滚动入场动画不适用于正文编辑器。
- 使用系统字体、SF Symbols、语义颜色和原生菜单 / sheet。稳定一致的界面比每次换一种“高级”风格更重要。
- 正文、插入点、选区和输入法候选不能为了视觉效果延迟出现或被动画打断。

## 内容与控制层

导航、工具栏和按钮使用系统材质；正文清晰、稳定，不叠自制玻璃背景或装饰噪点。通过文字层级和适量留白组织信息，不把每段文字包装成卡片。

沿用 Apple 的[材质指南](https://developer.apple.com/design/human-interface-guidelines/materials)和[Liquid Glass](https://developer.apple.com/documentation/technologyoverviews/liquid-glass)作为设计参考；涉及具体新 API 时检查当前 SDK 与官方文档。

Mac 保持键盘、菜单、焦点与窗口操作习惯；iPad 根据可用空间适配分栏、触控和硬件键盘；iPhone 优先单列和清楚的返回路径。浅深色、增加对比度、减少透明度、减少动态效果和 VoiceOver 都需要实际验证，使用语义 API 不等于验证完成。

## 图标系统

界面图标默认使用 [SF Symbols](https://developer.apple.com/design/human-interface-guidelines/sf-symbols)，由 SwiftUI 的 `Image(systemName:)` 或对应原生 API 加载。优先选择语义准确的系统 symbol，并使用系统提供的字重、尺寸、变体和渲染模式适配所在控件；不把 symbol 导出为普通图片后重新描边，也不为同一组操作混用不同图标库。

只有在 SF Symbols 缺少稳定、准确的产品语义，或某个 Weave 专属图标确实需要跨平台共享时，才使用项目自有矢量资源。自有图标保留单一 SVG 源文件和语义命名，通过受控的资源边界生成或接入各平台；默认作为 template image 使用语义颜色。不能为了一个常规图标引入整套第三方依赖，第三方图标库也不能直接扩散到业务视图。

新增或替换图标时检查目标系统版本的可用性，并为不满足部署目标的 symbol 提供语义相同的回退。至少核对 macOS、iPadOS 和 iOS 的 16 / 20 / 24 pt，小尺寸字重、文字基线、浅深色、增加对比度、普通 / 悬停 / 按下 / 选中 / 禁用状态，以及无文字按钮的辅助功能标签。平台布局可以不同，同一操作的语义和视觉家族保持一致。

## 记录标题

记录标题在编辑区顶部使用独立的原生输入框，空值显示「无标题」，不属于正文富文本。标题是一段连续文本，随宽度自然折行并增长到四行；Return 进入正文，粘贴内容中的换行折叠为空格。标题与正文共用系统文本画布底色，浅深色切换时不能在二者之间出现整块色差。标题与正文的文字起点对齐，使用系统 Semibold，字号为正文的 2 倍（Small 档为 28 pt），额外行距按字号比例计算（Small 档为 3 pt）；H1 保持 24 pt，以字号层级区分页面身份与正文章节。标题下方 4 pt 加正文顶部 16 pt，形成 20 pt 的内容间隔。新正文默认使用普通正文样式。侧栏、窗口标题和导出文件名保持单行表示，空标题显示「未命名记录」。导出 Markdown 不追加标题内容；正文中的 H1–H6 保持普通文档语义，不要求首段为 H1。

## 文档样式与输入准则

| 内容 | 设计要求 |
| --- | --- |
| 正文与空行 | 清楚易读；空行按字体自然高度排版，不设小于字体的行高上限来制造留白 |
| 标题 | 作用于完整段落；层级可辨；回车恢复正文，不污染后续输入 |
| 行内格式 | 只改变选中 / 标记内容；闭合后正常续写；将一段行内格式完整删除后，后续输入恢复当前段落的普通文字属性 |
| 列表 | 标记和换行悬挂对齐；续写、退出、缩进与退格行为可预测 |
| 引用 | 连续竖线、次要文字色和适度缩进；竖线按完整行框绘制并在引用首尾略微延伸，保留呼吸感；引用作为外层语义，可包含标题、Todo、有序/无序列表和代码块；Markdown 标记不额外占据文字宽度，装饰不干扰选区、复制与点击 |
| 行内代码 | 等宽、贴合文字的浅底色；与同一行正文基线协调 |
| 代码块 | 多行共用连续背景和内边距；当前块可选语言和复制，高亮不改变文字；边界、空行退出与撤销明确 |
| 表格 | 正文原位编辑，首行表头；行列与对齐操作就近显示；窄宽横向滚动，Tab 跟随单元格，不挤压文字 |
| 勾选项 | 文档与后续 Todo List 共用大圆角方形完成控件；标记可点击，有完成反馈；不擅自排序，不与拖动选字冲突 |
| 链接 | 可辨认、可编辑、可移除；选中文字粘贴 URL 保留原标签 |

跨中文、英文、emoji 和段落边界测试选区。中文组合输入期间不强制重排替换或移动光标。撤销既恢复内容，也恢复继续输入所需的属性；切换记录与重启不能改变格式。

## 当前排版基线

排版值集中在 `DocumentTypography`（`ParagraphEditing.swift`）；正文、标题、表格、代码及平台容器引用该配置，调整时不能只改文档：

- 阅读宽度约 728 pt，滚动容器铺满编辑区域，滚动条贴右。Mac 最小左右边距 32 pt，移动端 24 pt。
- Mac 默认采用 Small 阅读大小：显示字体倍率 14 / 13，默认正文约 14 pt，表格与正文使用同一字体解析和倍率；移动端倍率 1.08。显示与写回倍率互逆，不改写存储字号。当前尚无三档切换入口；Dynamic Type 的完整适配仍需真机核查。
- 正文与普通空段统一使用自然字体高度、6 pt 额外行距，段前为 0、段后为 6 pt。段内换行不增加段距；Mac 使用 Enter 换段、Shift+Enter 段内换行。
- Markdown 导入将两个块之间的一个分隔空行转换为段落边界，不再额外插入可编辑空段；更多空行保留为显式空段。连续正文源码行保留为段内换行，导出用两个尾随空格加换行表示；段落之间导出 Markdown 分隔空行。已有富文本空段保持原样，不猜测和删除历史留白。
- 普通文字的行框取字号 1.25 倍与中英文混排自然度量的较大值，再向上取整并固定为该段的最小 / 最大行高；空行预留同样高度，避免首次中文输入触发字体回退时跳动。表格等附件不受文字行高上限约束。
- 间距只由段落角色决定，不依赖该行或相邻行是否有字。输入首字、删除至空行时，下方内容位置应保持稳定。H1 / H2 段前 12 pt、段后 6 pt；H3 / H4 为 10 / 4 pt，H5 为 8 / 4 pt，H6 为 6 / 2 pt；文档首段标题不加段前距。显式设置为标题或引用的空段落沿用该角色的排版。
- 恢复光标输入属性时沿用当前段落排版；保留空格、制表符、字体和自然光标高度。
- Mac 光标、选区背景、闪烁与擦除由 NSTextView / NSLayoutManager 原生管理，不覆盖插入点绘制或选区填充。
- 排版区域保留阅读行距与段距；Mac 原生光标与选区仅保留各段落行距的三分之一作为字形下方呼吸空间（正文为 2 pt），其余额外行距不计入文字占用区域。代码首行的文字区域下移到工具栏下方；工具栏和背景依据布局区域定位，不能扩大原生光标与选区。
- 空段、首字输入和删除恢复使用系统文字区域；不拿换行控制字形的位置当文字基线，不另造光标坐标。检查文中及文末空段、软换行、标题和代码首行。
- 引用使用独立的 `QuoteAttribute` 叠加在段落角色之上，正文默认投影为系统次要文字色；显式文字色和代码语法色仍保留。竖线只由布局层绘制，正文不保存 `│ ` 占位字符；删除引用的全部文字时，空段仍依据输入属性显示竖线，输入 `> ` 后可继续使用标题、Todo、列表和围栏代码快捷语法，空引用内再次 Return 才退出引用。
- 行内代码采用 B 方案：系统等宽 Regular（显式强调保留），字号为所在正文或标题字号的 0.93 倍；文字使用与正文相同的动态语义色。背景按主题分别使用 6.5% 黑 / 12% 白的中性叠色。以代码实际字号为 1 em，圆角 0.46 em、上下内留白 0.14 em、左右内留白 0.5 em、左右外间距 0.3 em；正文和六级标题共用比例，不写死像素。背景垂直范围由基线、字体大写高度及下伸部确定，中英文和 emoji 实际字形可扩展背景；不使用整行布局框。灰底额外上移代码字号的 0.08 em，改善与正文的视觉居中；只改变装饰绘制，不移动文字、原生光标或选区。已有单个空格计入边界间隔，多空格和换行保留原样；间距使用原生字符排版，不插入空格、不改选区绘制，保存往返不累积字号或间距。
- 代码块水平内边距 14 pt、行间距 3 pt；相邻行共用块标识和连续表面。首行在 TextKit 布局中预留工具栏高度（Mac 32 pt、移动端 44 pt），不插入占位字符。语言和复制操作常驻块内，复制反馈原位显示；代码仍随正文宽度自动换行，尚无独立的不换行横向滚动模式。
- 代码块背景由已存储内容的行片段决定，不使用随当前输入属性变化的文末附加行范围。未退出的末尾代码空行保留固定空间；Mac 点击文末代码块下方进入正文，可撤销。导入时结束围栏后的换行使用正文属性，避免继承代码格式。
- 表格保留轻量菜单；激活或悬停时显示对齐和退出操作，显隐不改变高度。菜单提供同样的操作，支持触控和键盘访问。普通单元格保持正文底色，表头和网格采用低对比度语义色。
- Todo 正文只保存用户输入的文字，完成状态由 `TaskStateAttribute` 段落属性记录，并与 Markdown `- [ ] / - [x]` 往返；旧数据中的 `☐ / ☑` 前缀在读取时迁移并移除。显示控件使用按字号缩放的圆角方框和 SF Symbols `checkmark` 组合：圆角为图标边长的 28%，细边框在完成前后保持同一灰色，完成态只使用 Semibold 强调色勾，不添加内底色；勾向右补偿图标边长的 4%，以字形轮廓而非 Symbol 画布实现视觉居中；macOS 悬停时边框临时变为强调色。控件由布局层绘制在正文左侧，不进入文本内容、光标、选区或纯文本复制，并为辅助技术提供带任务文字和完成状态的切换动作。可见图标为所在文字字号的 1.08 倍，按字体度量与正文垂直居中，图标与正文保留 0.45 em 间距，首行正文与换行缩进保持 24 pt 对齐。光标最左只停在第一段文字前。点击热区在 macOS 至少 24 pt、触控平台至少 44 pt，悬停显示手型指针。Tab / Shift-Tab 一次调整一级并保留任务语义，Return 续写时沿用当前层级，不重复累加缩进。父子层级由缩进表达，后续独立 Todo List 沿用同一形状和状态语义。
- Small 档 H1–H6 为 24 / 20 / 17 / 15 / 14 / 14 pt，由正文基准与比例推导。H1–H5 使用 Semibold，H6 使用 Medium，标题额外行距统一为 3 pt；导入、快捷输入和段落菜单共用映射。段内加粗、斜体独立于标题字重记录，切换段落样式与 Markdown 往返保留这些强调。

## 审核与验收

先定位具体场景，再比较 Apple Notes / Bear 的原生写作与键盘体验，或 Craft / Notion 的段落组织。竞品行为需当前证据，不能凭印象宣称一致；只吸收适合本期的交互。

每条问题记录：场景、实际结果、预期、影响、修复建议。优先修复丢字、错选区、撤销或存储问题，其次输入阻碍，再处理样式精修。至少观察实际 App 的输入、选区和滚动状态；静态截图只证明视觉，不能证明输入行为。

完整操作矩阵见[验证清单](docs/verification.md)。后续画板进入独立编辑区并恢复正文位置，属于设计方向，当前没有实现。

代码背景的圆角留白不能跨过相邻代码范围的布局边界，避免半透明背景重叠。鼠标落在已有空段时，继续输入沿用该段落的属性，不从前一个代码块继承。

_Last updated: 2026-09-14 — 明确 SF Symbols 为默认图标系统，并规定自有图标的例外和验收边界。_
